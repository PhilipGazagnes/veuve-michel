import type { Config } from 'tailwindcss'

const config: Partial<Config> = {
  theme: {
    extend: {
      colors: {},
      screens: {
        xs: '480px',
      },
    },
  },
}

export default config
